# embedding_app
a local vector store suitable for **retrieval‑augmented generation** (RAG) systems.  It accepts documents from the file system, a Git repository or an Atlassian Confluence space, converts their contents into dense embeddings using a pluggable embedding service and indexes them using **FAISS**  
